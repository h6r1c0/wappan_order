# 検証結果と残る接続確認

2026年9月15日、main `d62e87b3aba927a38348e7fc9f5190f27e7d0248` のREADME・画像2点を基に実装。

## 自動検証

- `npm test`: 9テスト。価格切捨て・個別価格・固定注文・欠品・個人集金・混在納品書・行事余剰・原価振替・販売取消・過去実績・年度境界・目標・テスト除外・Excel実ファイルを検証。
- 同テスト内でPostgres（PGlite）へmigrationを適用。匿名アクセス拒否、係以外のアクセス拒否、RLS、直接更新拒否、RPC保存、revision競合拒否、振替上限、納品書の重複割当拒否、更新履歴の保持を確認。
- `npx playwright test`: 3つのブラウザー業務シナリオ。Excel取込→固定注文→集金→確定→欠品→精算→行事20/18/2→個人請求・即時入金→利益→リロード、複数端末の競合、入力途中の購入者追加、過去利益・調整・目標・テスト区分を確認。
- 320 / 375 / 430px幅で注文入力に横スクロールがないことを検証。375px幅の注文・行事・集計画面を画像で確認。検証環境では日本語フォントを補って確認し、本体は端末標準の丸ゴシック・ゴシックを使用。
- Supabase接続情報あり／未設定の両方で本番ビルドを確認。未設定時は接続案内だけを表示する。

## 実Supabaseでの確認（2026年9月15日）

- 東京リージョンの無料プロジェクト `vywgmxegeemlpsescika` を作成し、2件のmigrationを適用。公開キーを用いた接続設定で本番ビルド成功。
- 実Postgresで匿名アクセス拒否、係以外の読取・保存拒否、係のRPC保存と監査履歴作成、古いrevisionとNULL revisionの拒否、直接更新拒否を確認。検証トランザクションはROLLBACKし、Authユーザー0件・revision 0を確認。
- 実REST APIも未ログインの読取をHTTP 401 / 42501で拒否し、データが返らないことを確認。
- SECURITY DEFINER処理と係テーブルを非公開schemaへ移動し、公開RPCはSECURITY INVOKERに変更。security advisorの警告・エラーなし。係テーブルのRLSポリシーなしという情報通知は、直接アクセスを拒否する意図した設定。
- 移行後のローカルテスト9件も成功。初期マスター以外の業務データは登録していない。

## 実環境でまだ確認できない点

- 実サービスの利用者Authログイン、メール配信、実端末間共有はアカウント作成・公開後に確認する。
- ブラウザーテストのAuth/APIは検証用。実DB・RESTの確認とは区別し、利用者ログイン済みのブラウザーによる確認はまだ完了していない。
- 実際の毎月のExcel原本・9月11日注文内訳・納品書・手計算結果は未提供。その固有表の読取漏れや、実績との金額一致は未確認。
- iPhone実機のSafariでの最終的な指の押しやすさは所有者の端末で確認する。

[セットアップ](SETUP.md)の後、[9月11日分の照合](ACCEPTANCE.md)を行って本番利用へ移行する。
